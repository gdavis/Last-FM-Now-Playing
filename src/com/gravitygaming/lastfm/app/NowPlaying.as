package com.gravitygaming.lastfm.app {
	import flash.display.MovieClip;

	/**
	 * @author grantdavis
	 */
	public class NowPlaying extends MovieClip {
		public function NowPlaying() {
			trace("stage w: " + this.stage.stageWidth + ", h: " + this.stage.stageHeight );
			
			
		}
	}
}